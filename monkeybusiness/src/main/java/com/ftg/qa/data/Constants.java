package com.ftg.qa.data;

public class Constants {

	public enum Exchange 			{ BUY, SELL; }
	public enum Ordertype 			{ MARKET, LIMIT, STOP_MARKET, STOP_LIMIT; }
	public enum OrderRestriction 	{ FILL_OR_KILL, IMMEDIATE_OR_CANCEL; }
	public enum Masterdata			{ WEBDRIVER, ORDER; }
		
}
